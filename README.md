# Pra começar a rodar tudo no local, basta fazer o git clone desse repo, ou abaixar o .zip e depois extrair.

# Após o download, localize (de preferencia no terminal) o diretório jogos_teste/jogos/.
# localizado o diretório, execute o comando

docker-compose up -d

# para conferir se o docker subiu certinho execute:

docker ps

# após isso acesse no navegador:
http://localhost:4780

# é para tudo estar funcionando -_-
